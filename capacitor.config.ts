import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kyntrix.studio',
  appName: 'KyntrixStudio',
  webDir: '.output/public'
};

export default config;
